CREATE TABLE ARES_DAY(
SPACE_ID NUMBER NOT NULL,
A_MON NUMBER DEFAULT 0,
A_TUE NUMBER DEFAULT 0,
A_WED NUMBER DEFAULT 0,
A_THU NUMBER DEFAULT 0,
A_FRI NUMBER DEFAULT 0,
A_SAT NUMBER DEFAULT 0,
A_SUN NUMBER DEFAULT 0
);




CREATE TABLE ARES_DATE (
SPACE_ID NUMBER NOT NULL,
A_DATE DATE NOT NULL,
A_PRI NUMBER NOT NULL
);


MERGE INTO SPACE_LIST
USING DUAL
   ON (SPACE_ID = 40)
WHEN MATCHED THEN
    UPDATE SET
    
    
WHEN NOT MATCHED THEN
    INSERT(
	)
VALUES(
	)